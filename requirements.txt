# Core dependencies with wheel-only installations
opencv-python-headless==4.8.0.74 --only-binary :all:
numpy==1.24.3 --only-binary :all:
scikit-learn==1.3.0 --only-binary :all:
mediapipe==0.8.11 --only-binary :all:

# Web app requirements
streamlit>=1.24.0,<2.0.0
Pillow>=9.5.0,<10.0.0

# Support libraries
protobuf>=3.20.0,<4.0.0
six>=1.16.0
python-dotenv>=0.19.0